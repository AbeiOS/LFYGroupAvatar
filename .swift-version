
 -> echo -> echo
    - ERROR | spec: The specification defined in `echo` could not be loaded.

[!] No podspec exists at path `echo`.

[!] echo did not pass validation, due to 1 error.
You can use the `--no-clean` option to inspect any issue.
